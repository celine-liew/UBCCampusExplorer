{
    "title": "SELECT dept WHERE avg < 1 - only one column key (audit)",
    "query": {
        "WHERE": {
            "LT": {
                "courses_avg":1
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_audit"
            ],
            "ORDER": "courses_audit"
        }
    },
    "isQueryValid": true,
    "result": [
        { "courses_audit": 0 },
        { "courses_audit": 0 },
        { "courses_audit": 0 }
    ]
}
