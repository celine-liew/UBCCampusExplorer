{
    "title": "SELECT dept WHERE avg < 1 - only one column key (dept)",
    "query": {
        "WHERE": {
            "LT": {
                "courses_avg":1
            }
        },
        "OPTIONS": {
            "COLUMNS": [
                "courses_dept"
            ],
            "ORDER": "courses_dept"
        }
    },
    "isQueryValid": true,
    "result": [
        { "courses_dept": "frst" },
        { "courses_dept": "lfs" },
        { "courses_dept": "lfs" }
    ]
}
